import 'package:flutter/material.dart';

class UserCommentListScreen extends StatelessWidget {
  const UserCommentListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Users comments List '),),
    );
  }
}
